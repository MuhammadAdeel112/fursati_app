import 'package:flutter/material.dart';

class AppColors {
  // Primary branding colors (from Fursatipro website)
  static const Color primary = Color(0xFF0056b3); // Deep Blue
  static const Color primaryDark = Color(0xFF003D82);
  static const Color primaryLight = Color(0xFF4DA3FF);
  
  // Background colors
  static const Color background = Color(0xFFFFFFFF); // White background
  static const Color inputBackground = Color(0xFFFFFFFF); // White for inputs
  
  // Text colors
  static const Color textPrimary = Color(0xFF1A1A1A); // Dark grey/black
  static const Color textSecondary = Color(0xFF6C757D); // Grey for sub-text
  static const Color textLight = Color(0xFFFFFFFF);
  
  // UI Elements
  static const Color border = Color(0xFFE0E0E0); // Light grey for borders
  static const Color cardBackground = Color(0xFFF8FAFE); // Very light blue-white for cards
  static const Color error = Color(0xFFDC3545); // Red for error states
  static const Color success = Color(0xFF28A745); // Green for success
  static const Color link = Color(0xFF0056b3); // Same as primary for links
}
